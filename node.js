/**
 * module node
 */

define('node', ['$support', '$data', '$query'], function(support) {
    'use strict';
	
	var node = {
	    
		text: function(elem, item) {
		    
		}
		
	};
	
	doly.mixin(node);
	return node;
});