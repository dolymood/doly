/**
 * module node
 */

define('node', ['$support', '$data', '$query'], function(support) {
    'use strict';
	
	var node = {
	    
		text: function(elem, item) {
		    if (doly.isArrayLike(elem)) {
			    
			}
		},
		
		access: function() {
		    
		}
		
	};
	
	doly.mixin(node);
	return node;
});